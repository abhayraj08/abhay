function [ indexx ] = shifting...
    ( signal_x, mid_point_of_t, To )
%UNTITLED13 Summary of this function goes here
%   Detailed explanation goes here

length_of_signal=length(signal_x);
delayed_signal=zeros(1, length_of_signal_x);
indexx=0;
for ii=1:length_of_signal
    if(t(ii)<=To)
        indexx=indexx+1;
    else
        break;
    end
end

if (To<0)
    newx_index=mid_point_of_t-index;
    count_a=0;
    for ii=
    end
end

end

